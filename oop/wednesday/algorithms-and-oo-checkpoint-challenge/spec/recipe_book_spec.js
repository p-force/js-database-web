const recipeBook = require("../recipe_book");

describe("RecipeBook", () => {
	describe("find_recipe_by_id", () => {
		it("finds a recipe by id", () => {

		})

		it("raises an exception if it can't find a recipe", () => {

		})


	});

});